<h3>お気に入りタグ</h3>
<br>
<p>新しいタグを作成</p>
<!--　formはlink_to+modalで下記にJS記述してmodalウィンドウで対応 -->
<!--<%= form_with model: @new_tags do |form| %>
    <div class="field">
        <%= form.text_field :tag_name %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>
-->

お気に入りタグ<br>
<table>
  <thead>
    <tr>
      <th>タグ名</th>
      <th>順番</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @tag_users.each do |t| %>
      <tr>
        <td><%= t.tag.tag_name %></td>
        <td><%= t.position %></td>
        <td>削除</td>
      </tr>
    <% end %>
  </tbody>
</table>
