<h3>お気に入りタグ</h3>
<br>
<p>新しいタグを作成</p>
<%= form_with model: @new_tags do |form| %>
    <div class="field">
        <%= form.text_field :tag_name %>
    </div>
    <div class="actions">
        <%= form.submit %>
    </div>
<% end %>


お気に入りタグを並べる
<% binding.pry %>
<% @tag_users.each do |tag| %>
    <%= tag.tag_name %>　<%= tag.position %>
<% end %>
